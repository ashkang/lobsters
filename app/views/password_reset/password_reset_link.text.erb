سلام <%= @user.email %>,

یک نفر به آدرس <%= @ip %> تقاضای بازیابی پسورد شما در سرویس
<%= Rails.application.name %> رو کرده. اگر خودتون این تقاضا رو ارائه دادید با کلیک کردن روی لینک پایین پسوردتون رو بازیابی کنید. در غیر اینصورت
ایمیل رو نادیده بگیرید یا اون رو پاک کنید.

<%= Rails.application.root_url %>login/set_new_password?token=<%= @user.password_reset_token %>
