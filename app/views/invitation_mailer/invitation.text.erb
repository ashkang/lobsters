<%= سلام @invitation.email %>,

کاربر <%= @invitation.user.username %> شما رو به سرویس <%= Rails.application.name %> دعوت کرده. (<%= Rails.application.root_url %>)<%= @invitation.memo.present? ? raw("\n\n   #{@invitation.memo}") : "" %>

برای قبول این دعوت نامه و ساختن حساب کاربری روی لینک زیر کلیک کنید:

<%= Rails.application.root_url %>invitations/<%= @invitation.code %>
