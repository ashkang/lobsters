<%= @comment.user.username %> به شما جواب داده:

  <%= word_wrap(@comment.plaintext_comment, :line_width => 72).gsub(/\n/, "\n  ") %>

با کلیک کردن روی لینک روبرو میتونید به بحث ادامه بدید: <%= @comment.url %>
