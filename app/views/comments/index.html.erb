<ol class="comments comments1">
  <% @comments.each do |comment| %>
    <li><%= render "comments/comment", :comment => comment,
      :show_story => true %>
      <ol class="comments"></ol>
    </li>
  <% end %>
</ol>

<div class="morelink">
  <% if @page && @page > 1 %>
    <a href="/comments<%= @page == 2 ? "" : "/page/#{@page - 1}" %>">&lt;&lt; 
      صفحه
      <%= (to_farsi_number(@page - 1).to_s) %></a>
  <% end %>

  <% if @comments.any? %>
    <% if @page && @page > 1 %>
      |
    <% end %>

    <a href="/comments/page/<%= @page + 1 %>">صفحه
      <%= (to_farsi_number(@page + 1).to_s) %> &gt;&gt;</a>
  <% end %>
</div>
